<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:quality_courses, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:quality_courses, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:quality_course) %></h2>
    <div class="inner">
      <% form_for :quality_course, url(:quality_courses, :create), :class => :form do |f| %>
        <%= partial "quality_courses/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
