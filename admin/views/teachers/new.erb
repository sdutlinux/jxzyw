<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:teachers, :index) %></li>
      <li class="active"><%= link_to pat(:new), url(:teachers, :new) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:new) %> <% mt(:teacher) %></h2>
    <div class="inner">
      <% form_for :teacher, url(:teachers, :create), :class => :form do |f| %>
        <%= partial "teachers/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
