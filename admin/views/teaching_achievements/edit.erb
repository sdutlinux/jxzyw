<div class="block">
  <div class="secondary-navigation">
    <ul class="wat-cf">
      <li class="first"><%= link_to pat(:list), url(:teaching_achievements, :index) %></li>
      <li><%= link_to pat(:new), url(:teaching_achievements, :new) %></li>
      <li class="active"><%= link_to pat(:edit), url(:teaching_achievements, :edit, :id => @teaching_achievement.id) %></li>
    </ul>
  </div>
  <div class="content">
    <h2 class="title"><%= pat(:edit) %> <% mt(:teaching_achievement) %></h2>
    <div class="inner">
      <% form_for :teaching_achievement, url(:teaching_achievements, :update, :id => @teaching_achievement.id), :method => :put, :class => :form do |f| %>
        <%= partial "teaching_achievements/form", :locals => { :f => f } %>
      <% end %>
    </div>
  </div>
</div>
<% content_for :sidebar, partial("base/sidebar") %>
