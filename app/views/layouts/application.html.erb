 <!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "教学资源网" %></title>
    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "bootstrap.min" %>
    <%= stylesheet_link_tag "custom" %>
    <%= stylesheet_link_tag "docs" %>

  </head>
  <body>

    <div class="navbar navbar-fluid-top navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container-fluid">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <a class="brand" href="/">山东理工大学教学资源网</a>
          <div class="container-fluid nav-collapse">
            <ul class="nav pull-right">
              <li><%= link_to "管理登录", '/admin/sessions/new' %></li>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container-fluid">
      <div class="row-fluid">
      <!-- 
        <div class="span2">
          <div class="well sidebar-nav">
            --> 
            <div class="span2 bs-docs-sidebar">
              <ul class="nav nav-list bs-docs-sidenav affix">
              <% @sections.each do |s| %>
                <li class=<%= 'active' if s.key == @key  %>>
                  <%= link_to  s.name + '<i class="icon-chevron-right"></i>' , url(:section, :index, s.key ) %>
                </li>
              <% end %>
              </ul>
            </div>


          <!-- 
            <ul class="nav nav-list">
              <% @sections.each do |s| %>
                <li class="nav-header">  
                <i class="icon-list"></i>
                <%= link_to  s.name, url(:section, :index, s.key ), :class => 'section-nav' %>
                </li>
                  <% s.categories.each do |cate| %>
                <li style=<%= "display:none;" unless s.key == @key %>> <%=link_to cate.name, url(:category, :index, cate.id, s.key) %> </li>
                <% end %>
              <% end %>
            </ul>
          </div>
        </div>
        -->
        <div class="span10">
          <%= yield %>
        </div>
      </div><!--/row-->

      <footer>
        <p>&copy; Company 2012</p>
      </footer>

    </div> <!-- /container -->

    <%= javascript_include_tag  'jquery-1.8.2.min' %>
    <%= javascript_include_tag  'application' %>

  </body>
</html>
